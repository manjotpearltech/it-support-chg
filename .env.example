# ============================================================================
# AZURE OPENAI DIRECT INTEGRATION - CONFIGURATION
# ============================================================================
# This configuration calls Azure OpenAI directly from the frontend.
# No backend server needed - perfect for Cloudflare Pages deployment.
#
# IMPORTANT: These variables are exposed to the frontend.
# Use Azure AD authentication with Bearer tokens for security.

# Frontend - Azure OpenAI Configuration
# ============================================================================
# Azure OpenAI Service Endpoint
REACT_APP_AZURE_OPENAI_ENDPOINT=https://manjo-mgzmovnn-eastus2.openai.azure.com/

# Azure OpenAI Deployment ID
REACT_APP_AZURE_OPENAI_DEPLOYMENT=azcaitsupportopenai-gpt-4o-mini

# Azure OpenAI API Version
REACT_APP_AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Azure Access Token (Bearer token for authentication)
# Get token using: az account get-access-token --resource https://cognitiveservices.azure.com/
# IMPORTANT: Set this as a GitHub Secret in production, NOT in .env
REACT_APP_AZURE_ACCESS_TOKEN=your-azure-access-token

# Optional: Azure Blob Storage (for document viewer proxy)
AZURE_BLOB_CONNECTION_STRING=your-blob-connection-string
AZURE_BLOB_CONTAINER=your-container-name
ALLOWED_BLOB_HOSTS=your-storage-account.blob.core.windows.net
AZURE_BLOB_SAS=your-sas-token

# Server Configuration
PORT=5001
MOCK_MODE=false

